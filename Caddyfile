{
	# make Caddy listen where docker publishes
	http_port 8080
	https_port 8443

	# internal CA for Tailscale/private
	pki {
		ca internal
	}
}

# hostnames (MagicDNS + IP)
orion-chrysalis, 100.82.12.97 {
	tls internal
	reverse_proxy voice-app:8080
}
